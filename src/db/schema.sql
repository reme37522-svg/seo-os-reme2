-- MOVED. The schema now lives in dashboard/migrations/ (Wrangler D1 migrations,
-- applied automatically by the deploy scripts). Do not edit this file.
-- Fresh database: `npm run db:local` (local) or it happens on `npm run deploy`.
